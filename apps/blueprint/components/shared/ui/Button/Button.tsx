// Legacy Blueprint path retained as a compatibility reexport.
// New code must import Button from @repo/ui.
export {
  Button,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
} from "@repo/ui";
