import type { DataFreshness, MetricObservationBatch } from "./models";
export class DataFreshnessService { constructor(private readonly staleAfterMilliseconds = 48 * 60 * 60 * 1000) {} evaluate(latest: MetricObservationBatch | null, now: string): DataFreshness { if (!latest) return { state: "unavailable", partialCoverage: false }; const ageMilliseconds = Date.parse(now) - Date.parse(latest.observedAt); if (!Number.isFinite(ageMilliseconds) || ageMilliseconds < 0) return { state: "unavailable", partialCoverage: latest.availability === "partial" }; return { state: ageMilliseconds > this.staleAfterMilliseconds ? "stale" : "current", latestObservedAt: latest.observedAt, ageMilliseconds, partialCoverage: latest.availability === "partial" || latest.truncated }; } }
